@extends('layouts.app')

@section('content')
<div class="max-w-7xl mx-auto px-6 py-10">
    <div class="flex items-center justify-between mb-6">
        <h1 class="text-2xl font-semibold">Devotionals</h1>
        @auth
            <a href="{{ route('devotionals.create') }}"
               class="inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-4 py-2 text-white hover:bg-indigo-700">
               + New
            </a>
        @endauth
    </div>

    @if ($devos->count())
        <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">
            @foreach ($devos as $d)
                <article class="rounded-2xl border bg-white p-5 shadow-sm hover:shadow-md transition">
                    <a href="{{ route('devotionals.show', $d) }}" class="block">
                        <h2 class="font-semibold text-lg leading-tight">{{ $d->title }}</h2>
                        <p class="mt-1 text-xs text-gray-500">
                            {{ optional($d->published_at)->format('M j, Y') }}
                        </p>
                        @if($d->excerpt)
                            <p class="mt-3 text-sm text-gray-700 line-clamp-3">{{ $d->excerpt }}</p>
                        @endif
                    </a>

                    @auth
                        <div class="mt-4 flex items-center gap-3">
                            <a href="{{ route('devotionals.edit', $d) }}"
                               class="text-sm rounded-lg border px-3 py-1 hover:bg-gray-50">Edit</a>
                            <form method="POST" action="{{ route('devotionals.destroy', $d) }}">
                                @csrf @method('DELETE')
                                <button class="text-sm rounded-lg border px-3 py-1 hover:bg-red-50 text-red-600">
                                    Delete
                                </button>
                            </form>
                        </div>
                    @endauth
                </article>
            @endforeach
        </div>

        <div class="mt-8">
            {{ $devos->onEachSide(1)->links() }}
        </div>
    @else
        <div class="rounded-2xl border bg-white p-8 text-center">
            <p class="text-gray-600">No devotionals yet.</p>
            @auth
                <a href="{{ route('devotionals.create') }}"
                   class="mt-4 inline-flex rounded-xl bg-indigo-600 px-4 py-2 text-white hover:bg-indigo-700">
                   Add the first devotional
                </a>
            @endauth
        </div>
    @endif
</div>
@endsection
